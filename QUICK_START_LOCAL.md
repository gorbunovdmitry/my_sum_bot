# ⚡ Быстрый старт: Локальная модель на Mac M3 Pro

## 🎯 Текущий статус

✅ **Готово:**
- Python 3.11.5 установлен
- PyTorch 2.8.0 установлен
- MPS (Metal Performance Shaders) доступен
- .env файл создан

⚠️ **Нужно сделать:**
- Установить зависимости
- Настроить .env с Telegram API
- Протестировать модель

---

## 🚀 Быстрая установка (3 команды)

### 1. Установите зависимости:

```bash
cd telegram_summary_bot
./install_and_check.sh
```

Или вручную:
```bash
pip3 install -r requirements.txt
```

**Время:** 3-5 минут (зависимости для ML большие)

---

### 2. Настройте .env файл:

Откройте `.env` в редакторе и заполните:

```env
# Получите на https://my.telegram.org/apps
TELEGRAM_API_ID=ваш_api_id
TELEGRAM_API_HASH=ваш_api_hash

# Получите у @BotFather
BOT_TOKEN=ваш_bot_token

# Локальная модель (уже настроено)
OPENAI_API_KEY=
LOCAL_MODEL_PATH=facebook/bart-large-cnn
```

**Как получить:**
- **API ID/Hash:** https://my.telegram.org/apps → Create application
- **Bot Token:** @BotFather в Telegram → `/newbot`

---

### 3. Протестируйте модель:

```bash
python3 setup_local_model.py
```

**Что произойдет:**
- Загрузится модель `facebook/bart-large-cnn` (~1.6 GB первый раз)
- Протестируется производительность
- Покажет время обработки

**Ожидаемый результат:**
- ✅ Время: 5-15 секунд для 1000-5000 слов
- ✅ Память: 2-4 GB
- ✅ MPS используется автоматически

---

## 📋 Полная настройка (после установки зависимостей)

### Шаг 1: Настройте пользователя

```bash
python3 setup_user.py
```

Введите:
1. Номер телефона (+79991234567)
2. Код из Telegram
3. Выберите чаты (например: `1,3,5-10` или `all`)

### Шаг 2: Запустите бота

```bash
python3 main.py
```

Бот:
- Загрузит модель (если еще не загружена)
- Начнет сканирование по расписанию
- Отправит сводки автоматически

### Шаг 3: Проверьте работу

В Telegram:
- Найдите вашего бота
- `/start` - регистрация
- `/status` - проверить настройки
- `/summary` - получить сводку

---

## 🔧 Если что-то не работает

### Проблема: "ModuleNotFoundError"
```bash
pip3 install transformers torch sentencepiece python-telegram-bot telethon sqlalchemy pydantic-settings loguru
```

### Проблема: Модель не загружается
- Проверьте интернет (модель скачивается первый раз)
- Проверьте место на диске (~2 GB)

### Проблема: "Invalid API ID"
- Проверьте .env файл
- Убедитесь, что нет лишних пробелов
- Перепроверьте значения на my.telegram.org

---

## 📊 Проверка готовности

```bash
# Проверка Python и PyTorch
python3 -c "import torch; print(f'PyTorch {torch.__version__}, MPS: {torch.backends.mps.is_available()}')"

# Проверка зависимостей
python3 -c "import transformers; print('✅ Transformers OK')"
python3 -c "import telegram; print('✅ Telegram Bot OK')"
python3 -c "import telethon; print('✅ Telethon OK')"

# Проверка конфигурации
python3 -c "from config import settings; print(f'Модель: {settings.local_model_path}')"
```

---

## 🎯 Что дальше?

1. **Установите зависимости:** `./install_and_check.sh`
2. **Настройте .env:** Добавьте Telegram API credentials
3. **Протестируйте модель:** `python3 setup_local_model.py`
4. **Настройте пользователя:** `python3 setup_user.py`
5. **Запустите бота:** `python3 main.py`

---

## 💡 Полезные команды

```bash
# Тест производительности модели
python3 setup_local_model.py

# Тест масштабируемости
python3 test_scaling.py

# Просмотр логов
tail -f logs/bot.log

# Проверка использования памяти
top -pid $(pgrep -f "python3 main.py")
```

---

**Готово!** Следуйте шагам выше и ваш бот с локальной моделью заработает! 🚀
