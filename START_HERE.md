# 🚀 Начните отсюда: Быстрый старт с локальной моделью

## ✅ Текущий статус

- ✅ Python 3.11.5 установлен
- ✅ PyTorch 2.8.0 установлен  
- ✅ MPS (Metal Performance Shaders) доступен - ваш Mac M3 Pro готов!
- ✅ .env файл создан

---

## 📝 Шаг 1: Настройте .env файл

Откройте файл `.env` и заполните обязательные поля:

```env
# ⚠️ ОБЯЗАТЕЛЬНО: Получите на https://my.telegram.org/apps
TELEGRAM_API_ID=ваш_api_id_здесь
TELEGRAM_API_HASH=ваш_api_hash_здесь

# ⚠️ ОБЯЗАТЕЛЬНО: Получите у @BotFather в Telegram
BOT_TOKEN=ваш_bot_token_здесь

# ✅ УЖЕ НАСТРОЕНО ДЛЯ ЛОКАЛЬНОЙ МОДЕЛИ:
OPENAI_API_KEY=
LOCAL_MODEL_PATH=facebook/bart-large-cnn

# ✅ ОПТИМИЗАЦИИ ДЛЯ MAC M3 PRO:
MAX_WORKERS=4
ENABLE_CACHING=true
USE_MPS=true
```

**Как получить Telegram API:**
1. Откройте https://my.telegram.org/apps
2. Войдите с номером телефона
3. Создайте приложение
4. Скопируйте `api_id` и `api_hash`

**Как получить Bot Token:**
1. Найдите @BotFather в Telegram
2. Отправьте `/newbot`
3. Следуйте инструкциям
4. Скопируйте токен

---

## 📦 Шаг 2: Установите зависимости

```bash
cd telegram_summary_bot
pip3 install -r requirements.txt
```

**Это займет 2-5 минут** (зависимости для ML довольно большие)

---

## 🧪 Шаг 3: Протестируйте локальную модель

```bash
python3 setup_local_model.py
```

**Что произойдет:**
- Загрузится модель `facebook/bart-large-cnn` (~1.6 GB при первом запуске)
- Протестируется производительность
- Покажет время обработки и рекомендации

**Ожидаемый результат:**
- ✅ Время обработки: 5-15 секунд для 1000-5000 слов
- ✅ Использование памяти: 2-4 GB
- ✅ MPS будет использоваться автоматически

---

## 👤 Шаг 4: Настройте пользователя

```bash
python3 setup_user.py
```

**Вам нужно будет:**
1. Ввести номер телефона (например: +79991234567)
2. Ввести код подтверждения из Telegram
3. Выбрать чаты для сканирования:
   - Введите номера через запятую (например: 1,3,5-10)
   - Или `all` для всех чатов
   - Или `skip` чтобы пропустить (настроить позже)

---

## 🚀 Шаг 5: Запустите бота

```bash
python3 main.py
```

**Что произойдет:**
- Бот запустится и подключится к Telegram
- Локальная модель загрузится (если еще не загружена)
- Начнется планирование сканирования
- Бот будет ждать времени сканирования (по умолчанию 22:00 ±30 минут)

**Проверьте работу:**
1. Найдите вашего бота в Telegram (по username из BotFather)
2. Отправьте `/start`
3. Отправьте `/status` - проверьте настройки
4. Отправьте `/summary` - получите сводку (если уже есть данные)

---

## 📊 Шаг 6: Тест масштабируемости (опционально)

```bash
python3 test_scaling.py
```

Этот скрипт покажет:
- Пропускную способность вашего Mac M3 Pro
- Сколько пользователей может обслужить
- Рекомендации по оптимизации

---

## 🔧 Быстрая проверка

### Проверьте, что все работает:

```bash
# 1. Проверка Python и PyTorch
python3 -c "import torch; print(f'PyTorch {torch.__version__}, MPS: {torch.backends.mps.is_available()}')"

# 2. Проверка зависимостей
python3 -c "import transformers; print(f'Transformers {transformers.__version__}')"

# 3. Проверка конфигурации
python3 -c "from config import settings; print(f'Модель: {settings.local_model_path}')"
```

---

## ⚠️ Решение проблем

### Проблема: "ModuleNotFoundError"
**Решение:**
```bash
pip3 install transformers torch sentencepiece python-telegram-bot telethon
```

### Проблема: Модель не загружается
**Решение:**
- Проверьте интернет соединение (модель скачивается при первом запуске)
- Проверьте свободное место на диске (нужно ~2 GB)

### Проблема: "Invalid API ID"
**Решение:**
- Проверьте правильность `TELEGRAM_API_ID` и `TELEGRAM_API_HASH` в .env
- Убедитесь, что нет лишних пробелов

### Проблема: Бот не отвечает
**Решение:**
- Убедитесь, что `python3 main.py` запущен
- Проверьте логи: `tail -f logs/bot.log`
- Проверьте правильность `BOT_TOKEN` в .env

---

## 📚 Дополнительная документация

- `SETUP_LOCAL.md` - Детальная инструкция по настройке
- `COST_ANALYSIS.md` - Анализ стоимости локальных моделей vs API
- `SCALING_ANALYSIS.md` - Анализ масштабируемости на Mac M3 Pro
- `README.md` - Полная документация проекта

---

## ✅ Чеклист готовности

- [ ] .env файл заполнен (API_ID, API_HASH, BOT_TOKEN)
- [ ] Зависимости установлены (`pip3 install -r requirements.txt`)
- [ ] Модель протестирована (`python3 setup_local_model.py`)
- [ ] Пользователь настроен (`python3 setup_user.py`)
- [ ] Бот запущен (`python3 main.py`)
- [ ] Бот отвечает в Telegram (`/start`, `/status`)

---

## 🎯 Следующий шаг

**Начните с Шага 1** - заполните `.env` файл с вашими Telegram API credentials!

После этого запустите:
```bash
python3 setup_local_model.py
```

Это проверит, что локальная модель работает на вашем Mac M3 Pro! 🚀
