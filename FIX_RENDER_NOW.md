# 🔧 СРОЧНО: Исправление проблемы с авторизацией на Render

## Проблема
API endpoint `/check-auth/<user_id>` возвращает 404 на Render, хотя код есть в репозитории.

## Причина
Render не обновил код автоматически или SQLite на Render не работает (файловая система read-only).

## Решение

### Вариант 1: Перезапустить Render сервис (быстро)

1. Откройте https://dashboard.render.com
2. Найдите ваш сервис: `telegram-summary-bot-auth`
3. Нажмите **"Manual Deploy"** → **"Deploy latest commit"**
4. Дождитесь завершения деплоя (2-5 минут)

### Вариант 2: Настроить PostgreSQL (правильно)

SQLite на Render не работает! Нужно использовать PostgreSQL.

#### Шаг 1: Создайте PostgreSQL на Render

1. Render Dashboard → **New +** → **PostgreSQL**
2. Название: `telegram-summary-bot-db`
3. План: **Free**
4. Нажмите **Create Database**

#### Шаг 2: Получите Database URL

1. Откройте созданную базу данных
2. В разделе **Connections** → **Internal Database URL**
3. Скопируйте URL (например: `postgresql://user:pass@host:5432/dbname`)

#### Шаг 3: Обновите переменные окружения

1. Откройте Web Service: `telegram-summary-bot-auth`
2. **Environment** → найдите `DATABASE_URL`
3. Замените на PostgreSQL URL из шага 2
4. **Save Changes**
5. Render автоматически перезапустит сервис

#### Шаг 4: Обновите локальный .env

В файле `.env`:
```bash
DATABASE_URL=postgresql://user:pass@host:5432/dbname
```

**Важно**: Используйте тот же URL, что и на Render!

#### Шаг 5: Перезапустите бота

```bash
pkill -f "python3 main.py"
python3 main.py
```

## Проверка

После настройки PostgreSQL:

1. Отправьте боту `/auth`
2. Авторизуйтесь через GitHub Pages
3. Отправьте `/status` - должен показать "✅ Авторизован"

## Тестирование API

```bash
# Health check (должен вернуть 200)
curl https://telegram-summary-bot-auth.onrender.com/health

# Проверка авторизации (замените USER_ID на ваш Telegram ID)
curl https://telegram-summary-bot-auth.onrender.com/check-auth/107544557
```

Ваш Telegram ID можно узнать у @userinfobot в Telegram.

## Если все еще не работает

1. Проверьте логи Render: Dashboard → ваш сервис → **Logs**
2. Ищите ошибки связанные с БД
3. Убедитесь, что PostgreSQL URL правильный
4. Проверьте, что таблицы созданы (Render автоматически создаст при первом запуске)
