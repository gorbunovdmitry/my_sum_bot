# 🔍 Диагностика проблемы с ботом

## ✅ Что сделано

1. ✅ Бот перезапущен с логированием
2. ✅ Добавлено логирование команд
3. ✅ Исправлена проблема с множественными экземплярами

## 🔍 Как проверить

### Шаг 1: Убедитесь, что бот запущен

```bash
ps aux | grep "python3 main.py" | grep -v grep
```

Должен быть один процесс.

### Шаг 2: Отправьте команду боту

1. Откройте Telegram
2. Найдите бота: **@My_sum_test_bot**
3. Отправьте команду: `/start`

### Шаг 3: Проверьте логи

```bash
tail -f /tmp/bot_debug.log
```

Вы должны увидеть:
```
INFO | Получена команда /start от пользователя <ID>
```

## 🐛 Возможные проблемы

### Проблема 1: Бот не отвечает

**Причина:** Бот не получает обновления

**Решение:**
1. Проверьте, что бот запущен: `ps aux | grep python3 main.py`
2. Проверьте логи на ошибки: `tail -50 /tmp/bot_debug.log`
3. Убедитесь, что используете правильный username бота: **@My_sum_test_bot**

### Проблема 2: "Conflict: terminated by other getUpdates"

**Причина:** Запущено несколько экземпляров бота

**Решение:**
```bash
pkill -9 -f "python3 main.py"
sleep 2
cd telegram_summary_bot
python3 main.py > /tmp/bot_debug.log 2>&1 &
```

### Проблема 3: Бот не видит команды

**Причина:** Неправильный username или бот не запущен

**Решение:**
1. Проверьте username в BotFather
2. Убедитесь, что используете правильный username: **@My_sum_test_bot**
3. Перезапустите бота

## 📋 Команды для диагностики

```bash
# Проверить статус бота
ps aux | grep "python3 main.py" | grep -v grep

# Посмотреть логи
tail -50 /tmp/bot_debug.log

# Перезапустить бота
pkill -9 -f "python3 main.py"
cd telegram_summary_bot
python3 main.py > /tmp/bot_debug.log 2>&1 &

# Проверить, что бот получает обновления
curl -s "https://api.telegram.org/bot8446491207:AAF-3mzeHGLs0XXgddODNmZI66gJK0Tqu4k/getUpdates" | python3 -m json.tool
```

## ✅ Ожидаемое поведение

После отправки `/start` бот должен ответить:

```
🤖 Добро пожаловать в Telegram Summary Bot!

Этот бот поможет вам получать ежедневные сводки из ваших чатов и каналов.

🔐 Для начала работы необходимо авторизоваться:
Используйте команду /auth для авторизации через ваш Telegram аккаунт.
...
```

Если этого не происходит, проверьте логи!
