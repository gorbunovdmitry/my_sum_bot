# Telegram Summary Bot

Бот для автоматического создания ежедневных сводок из ваших Telegram чатов и каналов с использованием AI.

## 🎯 Возможности

- ✅ Безопасное сканирование чатов с защитой от блокировки
- ✅ Автоматическая суммаризация сообщений через AI
- ✅ Персонализированные сводки на основе интересов
- ✅ Ежедневная доставка сводок в удобное время
- ✅ Инкрементальное обновление (только новые сообщения)
- ✅ Вариация времени сканирования для безопасности

## ⚠️ Важно: Что нужно от вас

### 1. Telegram API Credentials

**Обязательно нужно получить:**

1. **API ID и API Hash** (для Client API):
   - Перейдите на https://my.telegram.org/apps
   - Войдите с вашим номером телефона
   - Создайте новое приложение
   - Скопируйте `api_id` и `api_hash`

2. **Bot Token** (для Bot API):
   - Найдите @BotFather в Telegram
   - Отправьте `/newbot`
   - Следуйте инструкциям
   - Скопируйте токен бота

### 2. OpenAI API Key (опционально)

Для качественной суммаризации рекомендуется использовать OpenAI:
- Получите API ключ на https://platform.openai.com/api-keys
- Или используйте локальную модель (см. ниже)

### 3. База данных

**Вариант A: PostgreSQL (рекомендуется для продакшена)**
```bash
# Установите PostgreSQL и создайте БД
createdb telegram_summary
```

**Вариант B: SQLite (для разработки)**
- Работает из коробки, ничего настраивать не нужно

## 📦 Установка

### 1. Клонируйте репозиторий или создайте проект

```bash
cd telegram_summary_bot
```

### 2. Установите зависимости

```bash
pip install -r requirements.txt
```

### 3. Настройте переменные окружения

Скопируйте `.env.example` в `.env`:

```bash
cp .env.example .env
```

Отредактируйте `.env` и заполните:

```env
# Обязательно
TELEGRAM_API_ID=ваш_api_id
TELEGRAM_API_HASH=ваш_api_hash
BOT_TOKEN=ваш_bot_token

# Опционально (для OpenAI)
OPENAI_API_KEY=ваш_openai_key

# База данных (SQLite для начала)
DATABASE_URL=sqlite:///./data/summary_bot.db
```

### 4. Настройте пользователя

Запустите скрипт настройки:

```bash
python setup_user.py
```

Этот скрипт:
- Авторизует вас через Telegram
- Покажет список всех ваших чатов
- Позволит выбрать чаты для сканирования
- Сохранит настройки в БД

### 5. Запустите бота

```bash
python main.py
```

## 🔧 Конфигурация

### Настройки сканирования (в `.env`):

```env
# Время сканирования (базовое)
SCAN_BASE_HOUR=22          # 22:00
SCAN_BASE_MINUTE=0

# Вариация времени (±30 минут для безопасности)
SCAN_TIME_VARIATION_MINUTES=30

# Максимум чатов за раз (рекомендуется 50)
MAX_CHATS_PER_SCAN=50

# Задержки между запросами (секунды)
SCAN_DELAY_MIN_SECONDS=1
SCAN_DELAY_MAX_SECONDS=5

# Вероятность пропустить сканирование (5%)
SKIP_SCAN_PROBABILITY=0.05

# Максимум запросов в секунду (запас от лимита 30)
MAX_REQUESTS_PER_SECOND=20
```

## 🛡️ Безопасность

Бот реализует все рекомендации по снижению риска блокировки:

1. ✅ **Вариация времени** - сканирование происходит в случайное время в диапазоне ±30 минут
2. ✅ **Инкрементальное обновление** - запрашиваются только новые сообщения
3. ✅ **Ограничение количества** - максимум 50 чатов за раз
4. ✅ **Случайные задержки** - 1-5 секунд между запросами
5. ✅ **Случайный порядок** - чаты обрабатываются в случайном порядке
6. ✅ **Пропуски сканирования** - 5% вероятность пропустить день (имитация человеческого поведения)

**Риск блокировки с этими настройками: 10-20%** (приемлемый уровень)

## 📱 Использование

### Команды бота:

- `/start` - Начать работу с ботом
- `/help` - Справка
- `/summary` - Получить последнюю сводку
- `/status` - Проверить статус и настройки

### Автоматическая работа:

Бот автоматически:
1. Сканирует выбранные чаты каждый день в указанное время (с вариацией)
2. Создает сводку с помощью AI
3. Отправляет сводку вам в личные сообщения

## 🏗️ Архитектура

```
telegram_summary_bot/
├── main.py              # Главный модуль
├── config.py            # Конфигурация
├── database.py          # Модели БД
├── telegram_client.py   # Безопасный Client API клиент
├── scheduler.py         # Планировщик с вариацией времени
├── summarizer.py        # AI суммаризация
├── bot.py               # Bot API для доставки
├── setup_user.py        # Скрипт настройки пользователя
├── requirements.txt     # Зависимости
├── .env.example         # Пример конфигурации
└── README.md            # Документация
```

## 💰 Стоимость и требования

**Детальный анализ стоимости и требований:** См. `COST_ANALYSIS.md`

### Краткое резюме:

**Локальная модель:**
- ✅ Бесплатно (только электричество ~0.3-1.5₽/месяц)
- ✅ Работает на любом компьютере с 4GB+ RAM
- ⚠️ Медленнее чем API (30-300 секунд)

**API (OpenAI/Gemini):**
- 💰 2.4-5₽/месяц (Gemini/GPT-3.5)
- ✅ Быстро (2-5 секунд)
- ✅ Высокое качество

**Рекомендация:** Начните с локальной модели, переходите на API если нужна скорость.

### Тест производительности локальной модели:

```bash
python setup_local_model.py
```

Этот скрипт проверит, вытянет ли ваш компьютер локальную модель.

## 🔍 Детали реализации

### Безопасное сканирование

```python
# Пример использования SafeTelegramClient
client = SafeTelegramClient(user_id, phone)
await client.connect()

# Безопасное сканирование с автоматическими задержками
messages = await client.scan_chats_safe(
    chat_ids=[123, 456, 789],
    max_chats=50
)
```

### Планировщик с вариацией

```python
scheduler = SafeScheduler()

# Получить время следующего сканирования (с вариацией)
next_time = scheduler.get_next_scan_time()
# Например: 22:15:37 (вместо фиксированного 22:00:00)
```

## 🐛 Решение проблем

### Ошибка авторизации

- Проверьте правильность `api_id` и `api_hash`
- Убедитесь, что номер телефона указан с кодом страны (+7...)

### FLOOD_WAIT ошибки

- Это нормально при превышении лимитов
- Бот автоматически обрабатывает эти ошибки
- Если ошибки частые, увеличьте задержки в конфигурации

### Бот не отправляет сводки

- Проверьте, что бот запущен (`python main.py`)
- Убедитесь, что выбраны чаты для сканирования (`/status`)
- Проверьте логи в `logs/bot.log`

## 📊 Мониторинг

Логи сохраняются в:
- `logs/bot.log` - основной лог
- `logs/setup.log` - лог настройки

Уровень логирования настраивается в `.env`:
```env
LOG_LEVEL=INFO  # DEBUG, INFO, WARNING, ERROR
```

## 🔐 Конфиденциальность

- Сессии пользователей хранятся локально в `sessions/`
- Сообщения сохраняются в БД (можно настроить шифрование)
- Рекомендуется использовать PostgreSQL с шифрованием для продакшена

## 📝 Лицензия

MIT License

## 🤝 Поддержка

При возникновении проблем:
1. Проверьте логи
2. Убедитесь, что все настройки корректны
3. Проверьте документацию в `technical_analysis.md`

---

**Важно:** Этот бот использует Telegram Client API, что может нарушать Terms of Service при коммерческом использовании. Используйте на свой риск и только для личных целей.
