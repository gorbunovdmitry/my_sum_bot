# 🔧 Исправление: PostgreSQL URL для локального подключения

## Проблема
Internal Database URL от Render не доступен локально - он работает только внутри сети Render.

## Решение

### Вариант 1: Использовать External Database URL (рекомендуется)

1. Откройте PostgreSQL на Render
2. В разделе **Connections** найдите **External Database URL**
3. Скопируйте его (он будет с другим хостом, например: `dpg-xxx-a.frankfurt-postgres.render.com`)
4. Обновите `.env`:
   ```bash
   DATABASE_URL=postgresql://user:pass@dpg-xxx-a.frankfurt-postgres.render.com/telegram_summary_bot_db
   ```

### Вариант 2: Разные БД для локального и Render (текущее решение)

**Локально (бот):**
- Используйте SQLite: `DATABASE_URL=sqlite:///./data/summary_bot.db`
- Бот работает локально с SQLite

**На Render (auth_server):**
- Используйте PostgreSQL: `DATABASE_URL=postgresql://...` (Internal URL)
- Auth server работает на Render с PostgreSQL

**Синхронизация:**
- Бот синхронизирует авторизацию через API `/check-auth`
- После авторизации на Render, бот получает статус через API и сохраняет локально

### Вариант 3: Использовать External URL для обоих

Если хотите использовать одну БД для обоих:

1. Получите External Database URL из Render
2. Обновите `.env` локально на External URL
3. Обновите `DATABASE_URL` в Render Web Service на External URL (или оставьте Internal)

## Текущая настройка

Сейчас используется **Вариант 2**:
- Локально: SQLite
- Render: PostgreSQL
- Синхронизация через API

Это работает, но авторизация сохраняется в двух местах. Для полной синхронизации лучше использовать External URL.
