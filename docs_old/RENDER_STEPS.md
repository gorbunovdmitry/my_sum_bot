# 📝 Пошаговая инструкция: Render.com

## Шаг 1: Регистрация на Render

1. Откройте https://render.com
2. Нажмите **"Get Started for Free"**
3. Войдите через GitHub (рекомендуется)

## Шаг 2: Создание Web Service

1. В Dashboard нажмите **"New +"** → **"Web Service"**
2. Выберите **"Connect GitHub"** (если еще не подключен)
3. Выберите репозиторий: **`gorbunovdmitry/my_sum_bot`**
4. Нажмите **"Connect"**

## Шаг 3: Настройка сервиса

Заполните форму:

- **Name**: `telegram-summary-bot-auth`
- **Region**: Выберите ближайший (например, Frankfurt)
- **Branch**: `main`
- **Root Directory**: оставьте пустым
- **Runtime**: `Python 3`
- **Build Command**: `pip install -r requirements.txt`
- **Start Command**: `python3 auth_server.py`

## Шаг 4: Переменные окружения

Прокрутите вниз до раздела **"Environment Variables"** и добавьте:

```
TELEGRAM_API_ID = 38193776
TELEGRAM_API_HASH = dec3b5f4c65a6b6f11d78490470c60be
BOT_TOKEN = 8446491207:AAF-3mzeHGLs0XXgddODNmZI66gJK0Tqu4k
DATABASE_URL = sqlite:///./data/summary_bot.db
LOG_LEVEL = INFO
```

⚠️ **Важно**: Для каждого значения нажмите **"Add Environment Variable"**

## Шаг 5: Создание и деплой

1. Прокрутите вниз
2. Выберите план: **"Free"** (для начала)
3. Нажмите **"Create Web Service"**
4. Render начнет деплой (займет 2-5 минут)

## Шаг 6: Получение URL

После успешного деплоя вы увидите:

```
✅ Your service is live at:
https://telegram-summary-bot-auth.onrender.com
```

**Скопируйте этот URL!** Он понадобится для настройки фронтенда.

## Шаг 7: Обновление index.html

1. Откройте репозиторий на GitHub: https://github.com/gorbunovdmitry/my_sum_bot
2. Откройте файл `index.html`
3. Нажмите кнопку **"Edit"** (карандаш)
4. Найдите строку:
   ```javascript
   const BACKEND_URL = 'https://ваш-бэкенд-домен.com/auth/callback';
   ```
5. Замените на ваш Render URL:
   ```javascript
   const BACKEND_URL = 'https://telegram-summary-bot-auth.onrender.com/auth/callback';
   ```
6. Прокрутите вниз, введите commit message: `Update BACKEND_URL for Render`
7. Нажмите **"Commit changes"**

## Шаг 8: Включение GitHub Pages

1. В репозитории перейдите в **Settings** → **Pages**
2. В разделе **Source**:
   - Выберите **"Deploy from a branch"**
   - **Branch**: `main`
   - **Folder**: `/ (root)`
3. Нажмите **"Save"**

Через несколько минут страница будет доступна на:
```
https://gorbunovdmitry.github.io/my_sum_bot/
```

## Шаг 9: Регистрация домена в BotFather

1. Откройте @BotFather в Telegram
2. Отправьте: `/setdomain`
3. Выберите: `My_sum_test_bot`
4. Введите домен (без https://): `gorbunovdmitry.github.io`
5. BotFather подтвердит регистрацию

## Шаг 10: Проверка

1. Откройте: https://gorbunovdmitry.github.io/my_sum_bot/
2. Должна появиться кнопка **"Login with Telegram"**
3. Нажмите кнопку и авторизуйтесь
4. Проверьте, что авторизация прошла успешно

---

## ✅ Готово!

Теперь у вас:
- ✅ Фронтенд на GitHub Pages
- ✅ Бэкенд на Render.com
- ✅ Домен зарегистрирован в BotFather
- ✅ Telegram Login Widget работает

---

## 🔧 Troubleshooting

### Проблема: Render показывает ошибку при деплое

**Решение**: Проверьте логи в Render Dashboard → Logs. Частые причины:
- Не установлены зависимости (проверьте `requirements.txt`)
- Неправильный `Start Command` (должен быть `python3 auth_server.py`)
- Отсутствуют переменные окружения

### Проблема: "Service unavailable" после деплоя

**Решение**: 
- На бесплатном плане Render "засыпает" после 15 минут бездействия
- Первый запрос после "сна" может занять 30-60 секунд
- Это нормально для бесплатного плана

### Проблема: Кнопка не появляется на GitHub Pages

**Решение**:
1. Проверьте, что домен зарегистрирован в BotFather
2. Проверьте, что `BACKEND_URL` в `index.html` правильный
3. Откройте консоль браузера (F12) и проверьте ошибки

### Проблема: "bot domain invalid"

**Решение**:
1. Убедитесь, что домен зарегистрирован в BotFather
2. Домен должен быть без `https://` и без `/` в конце
3. Подождите несколько минут после регистрации
