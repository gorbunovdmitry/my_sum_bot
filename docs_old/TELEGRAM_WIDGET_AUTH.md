# 🔐 Авторизация через Telegram Login Widget

## ✅ Что реализовано

Реализована безопасная авторизация через **Telegram Login Widget** - официальный способ авторизации, который не блокируется Telegram.

## 🚀 Как это работает

1. Пользователь отправляет `/auth` боту
2. Бот отправляет кнопку "Авторизоваться через Telegram"
3. Открывается веб-страница с кнопкой "Login with Telegram"
4. Пользователь нажимает кнопку и подтверждает авторизацию
5. Данные отправляются на сервер, проверяется подпись
6. Пользователь авторизован!

## 📋 Компоненты

### 1. Веб-сервер (`auth_server.py`)
- Обрабатывает callback от Telegram Widget
- Проверяет подпись данных
- Сохраняет авторизацию в БД

### 2. HTML страница (`auth_page.html`)
- Содержит Telegram Login Widget
- Отправляет данные на сервер после авторизации

### 3. Обновленный бот (`bot.py`)
- Команда `/auth` отправляет кнопку с ссылкой на страницу авторизации

## 🔧 Настройка

### Для локальной разработки:
- Веб-сервер запускается на `http://localhost:5000`
- Бот автоматически запускает веб-сервер при старте

### Для продакшена:
1. Разместите веб-сервер на публичном домене (например, через ngrok или VPS)
2. Обновите URL в `bot.py`:
   ```python
   auth_url = "https://your-domain.com/auth"
   ```
3. Убедитесь, что домен добавлен в настройки бота в BotFather (если требуется)

## 📱 Использование

1. Отправьте `/auth` боту
2. Нажмите кнопку "🔐 Авторизоваться через Telegram"
3. На открывшейся странице нажмите "Login with Telegram"
4. Подтвердите авторизацию в Telegram
5. Готово! Вы авторизованы.

## ✅ Преимущества

- ✅ Официальный способ авторизации
- ✅ Не блокируется Telegram
- ✅ Безопасная проверка подписи
- ✅ Удобный интерфейс для пользователя
- ✅ Не требует ввода номера телефона и кода

## 🔒 Безопасность

- Проверка подписи через HMAC-SHA256
- Проверка времени авторизации (не старше 24 часов)
- Данные передаются через HTTPS (в продакшене)
