# 🔍 Отладка проблемы с callback авторизации

## Проблема
Telegram Login Widget получает данные пользователя, но авторизация не сохраняется.

## Что проверить

### 1. Проверьте логи Render

1. Откройте https://dashboard.render.com
2. Найдите сервис: `telegram-summary-bot-auth`
3. Перейдите в **Logs**
4. Откройте страницу авторизации и авторизуйтесь
5. Смотрите логи в реальном времени

**Ищите:**
- `Получен callback:` - должен появиться при авторизации
- `Авторизация пользователя` - должен появиться с вашим ID
- `Пользователь успешно авторизован` - должно появиться после сохранения
- Ошибки связанные с БД

### 2. Проверьте, что PostgreSQL работает

В логах Render не должно быть ошибок типа:
- `could not connect to server`
- `relation "users" does not exist`
- `permission denied`

### 3. Проверьте endpoint вручную

```bash
curl -X POST https://telegram-summary-bot-auth.onrender.com/auth/callback \
  -H "Content-Type: application/json" \
  -d '{"id":187544557,"first_name":"Test","auth_date":1738579200,"hash":"test"}'
```

Должен вернуть JSON (даже если подпись неверная).

### 4. Проверьте CORS

В консоли браузера (F12) проверьте:
- Нет ли ошибок CORS
- Отправляется ли POST запрос на `/auth/callback`
- Какой статус ответа (200, 400, 500?)

### 5. Проверьте проверку подписи

Проблема может быть в `verify_telegram_auth`. Проверьте:
- Правильно ли вычисляется hash
- Совпадает ли Bot Token

## Возможные проблемы

### Проблема 1: PostgreSQL не работает

**Симптомы:** Ошибки подключения к БД в логах Render

**Решение:**
1. Проверьте, что `DATABASE_URL` правильный в Render
2. Убедитесь, что PostgreSQL создан и работает
3. Проверьте Internal Database URL

### Проблема 2: Неверная подпись

**Симптомы:** В логах "Неверная подпись Telegram"

**Решение:**
1. Проверьте, что Bot Token правильный в Render
2. Убедитесь, что используется тот же токен, что в BotFather

### Проблема 3: CORS ошибка

**Симптомы:** В консоли браузера ошибка CORS

**Решение:**
1. Убедитесь, что `CORS(app)` включен в `auth_server.py`
2. Проверьте, что Flask-Cors установлен

### Проблема 4: Таблицы не созданы

**Симптомы:** Ошибка "relation users does not exist"

**Решение:**
1. Таблицы должны создаться автоматически при первом запуске
2. Если нет - перезапустите сервис на Render

## Проверка после исправления

1. Откройте страницу авторизации
2. Авторизуйтесь через Telegram
3. Проверьте логи Render - должно быть:
   ```
   Получен callback: {...}
   Авторизация пользователя 187544557
   Пользователь 187544557 успешно авторизован
   ```
4. Проверьте статус:
   ```bash
   curl https://telegram-summary-bot-auth.onrender.com/check-auth/187544557
   ```
   Должен вернуть: `{"authorized": true, ...}`

5. Отправьте боту `/status` - должно показать "✅ Авторизован"
