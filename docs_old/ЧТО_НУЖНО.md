# 📋 Что нужно от вас для запуска бота

## ✅ Обязательные шаги (5-10 минут)

### 1. Telegram API Credentials (2 минуты)

**Получите API ID и API Hash:**
1. Откройте https://my.telegram.org/apps
2. Войдите с вашим номером телефона (получите код в Telegram)
3. Нажмите "Create application"
4. Заполните форму:
   - App title: `Summary Bot` (любое)
   - Short name: `summarybot` (любое)
   - Platform: `Other`
5. **Скопируйте:**
   - `api_id` (число, например: 12345678)
   - `api_hash` (строка, например: abc123def456...)

**Создайте бота:**
1. Откройте Telegram, найдите @BotFather
2. Отправьте `/newbot`
3. Придумайте имя (например: `My Summary Bot`)
4. Придумайте username (например: `my_summary_bot`)
5. **Скопируйте токен** (выглядит как: `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`)

### 2. Настройте .env файл (1 минута)

```bash
cd telegram_summary_bot
cp .env.example .env
```

Откройте `.env` и вставьте ваши данные:

```env
TELEGRAM_API_ID=ваш_api_id
TELEGRAM_API_HASH=ваш_api_hash
BOT_TOKEN=ваш_bot_token
```

### 3. Установите зависимости (2 минуты)

```bash
pip install -r requirements.txt
```

### 4. Настройте пользователя (2 минуты)

```bash
python setup_user.py
```

Вам нужно будет:
- Ввести номер телефона (с кодом страны, например: +79991234567)
- Ввести код подтверждения из Telegram
- Выбрать чаты для сканирования (или `all` для всех)

### 5. Запустите бота

```bash
python main.py
```

Готово! 🎉

---

## 🔧 Опционально (для лучшей суммаризации)

### OpenAI API Key

Если хотите качественную суммаризацию через GPT:

1. Зарегистрируйтесь на https://platform.openai.com
2. Перейдите в API Keys
3. Создайте новый ключ
4. Добавьте в `.env`:
   ```env
   OPENAI_API_KEY=ваш_ключ_здесь
   ```

**Без OpenAI:** Будет использована простая суммаризация (работает, но менее качественно)

---

## 📊 Что происходит дальше?

После запуска `python main.py`:

1. ✅ Бот запускается и ждет время сканирования
2. ✅ Каждый день в 22:00 (±30 минут) сканирует выбранные чаты
3. ✅ Создает сводку с помощью AI
4. ✅ Отправляет сводку вам в личные сообщения

**Первая сводка:** Появится после первого сканирования (на следующий день в указанное время)

**Или запросите сразу:** Найдите вашего бота в Telegram и отправьте `/summary`

---

## ⚠️ Важные моменты

1. **Безопасность:** Бот настроен с безопасными параметрами (риск блокировки 10-20%)
2. **Время сканирования:** По умолчанию 22:00, можно изменить в `.env`
3. **Количество чатов:** Максимум 50 за раз (можно изменить в `.env`)
4. **База данных:** По умолчанию SQLite (файл `data/summary_bot.db`)

---

## 🆘 Проблемы?

### "Invalid API ID"
- Проверьте правильность `api_id` и `api_hash`
- Убедитесь, что нет лишних пробелов в `.env`

### "Phone number invalid"
- Используйте формат: `+79991234567` (с кодом страны)
- Без пробелов и скобок

### Бот не отвечает
- Убедитесь, что `python main.py` запущен
- Проверьте логи: `logs/bot.log`
- Проверьте правильность `BOT_TOKEN` в `.env`

---

## 📁 Структура проекта

```
telegram_summary_bot/
├── .env                    # Ваши настройки (создайте из .env.example)
├── main.py                 # Запуск: python main.py
├── setup_user.py           # Настройка: python setup_user.py
├── config.py               # Конфигурация
├── database.py             # Модели БД
├── telegram_client.py      # Клиент Telegram
├── scheduler.py            # Планировщик
├── summarizer.py           # AI суммаризация
├── bot.py                  # Bot API
├── requirements.txt        # Зависимости
├── QUICKSTART.md           # Быстрый старт
└── README.md               # Полная документация
```

---

**Готово к запуску!** Следуйте шагам выше и бот заработает за 5-10 минут.
