# 🔧 Исправление: Бот не видит авторизацию

## Проблема

Бот работает локально и использует локальную базу данных, а auth server на Render использует другую БД. Поэтому бот не видит авторизацию, которая сохраняется на Render.

## Решение

Добавлена синхронизация через API:
1. Auth server на Render теперь имеет endpoint `/check-auth/<user_id>` для проверки статуса
2. Бот проверяет авторизацию через этот API перед проверкой локальной БД
3. Если авторизация найдена на Render, бот синхронизирует статус в локальную БД

## Что нужно сделать

### 1. Перезапустить бота

```bash
# Остановите текущий процесс бота (Ctrl+C)
# Затем запустите снова:
cd telegram_summary_bot
python3 main.py
```

### 2. Проверить авторизацию

1. Отправьте боту `/start` (если еще не отправляли)
2. Отправьте `/auth` - бот должен синхронизировать статус
3. Отправьте `/status` - проверьте статус авторизации

### 3. Если все еще не работает

Проверьте логи бота:
```bash
tail -f logs/bot.log
```

И проверьте, что auth server доступен:
```bash
curl https://telegram-summary-bot-auth.onrender.com/check-auth/ВАШ_USER_ID
```

Замените `ВАШ_USER_ID` на ваш Telegram ID (можно узнать у @userinfobot в Telegram).

## Альтернативное решение (для продакшена)

Для продакшена рекомендуется использовать одну общую базу данных:

1. **Создайте PostgreSQL на Render**:
   - В Render Dashboard → New → PostgreSQL
   - Скопируйте Internal Database URL

2. **Обновите переменные окружения**:
   - В Render Web Service добавьте: `DATABASE_URL=<postgresql_url>`
   - В локальном `.env` добавьте: `DATABASE_URL=<postgresql_url>`

3. **Перезапустите оба сервиса**

Теперь бот и auth server будут использовать одну БД.
