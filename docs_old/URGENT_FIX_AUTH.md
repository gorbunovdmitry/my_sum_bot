# 🚨 СРОЧНО: Исправление проблемы с авторизацией

## Проблема
GitHub Pages работает ✅, но бот не видит авторизацию ❌

## Причина
**SQLite на Render НЕ РАБОТАЕТ!** Файловая система read-only → авторизация не сохраняется → бот не видит авторизацию.

## Решение: PostgreSQL на Render (5 минут)

### Шаг 1: Создайте PostgreSQL на Render

1. Откройте https://dashboard.render.com
2. Нажмите **"New +"** (вверху справа)
3. Выберите **"PostgreSQL"**
4. Заполните:
   - **Name**: `telegram-summary-bot-db`
   - **Plan**: **Free**
5. Нажмите **"Create Database"**

⏱️ Подождите 1-2 минуты.

### Шаг 2: Скопируйте Database URL

1. Откройте созданную базу данных
2. В разделе **"Connections"** → **"Internal Database URL"**
3. Нажмите на иконку копирования
4. URL будет выглядеть так:
   ```
   postgresql://user:password@host:5432/dbname
   ```

### Шаг 3: Обновите Render Web Service

1. В Render Dashboard найдите: `telegram-summary-bot-auth`
2. Откройте его
3. Перейдите в **"Environment"** (меню слева)
4. Найдите `DATABASE_URL`
5. Нажмите для редактирования
6. **Замените** значение на PostgreSQL URL из шага 2
7. Нажмите **"Save Changes"**

✅ Render автоматически перезапустит сервис (2-5 минут).

### Шаг 4: Обновите локальный .env

1. Откройте файл `.env` в проекте
2. Найдите строку `DATABASE_URL=`
3. **Замените** на PostgreSQL URL из шага 2:
   ```bash
   DATABASE_URL=postgresql://user:password@host:5432/dbname
   ```
4. **ВАЖНО**: Используйте тот же URL, что и на Render!

### Шаг 5: Перезапустите бота

```bash
cd telegram_summary_bot
pkill -f "python3 main.py"
python3 main.py
```

### Шаг 6: Проверьте авторизацию

1. Отправьте боту `/start` (если еще не отправляли)
2. Отправьте `/auth`
3. Откройте ссылку: https://gorbunovdmitry.github.io/my_sum_bot/
4. Нажмите **"Login with Telegram"**
5. После успешной авторизации отправьте `/status`
6. Должно показать: **"✅ Авторизован"**

## Проверка работы

### Проверка API:

```bash
curl https://telegram-summary-bot-auth.onrender.com/check-auth/107544557
```

Должен вернуть JSON с `authorized: true` (если авторизованы).

### Проверка локальной БД:

```bash
cd telegram_summary_bot
python3 -c "
from database import SessionLocal, User
db = SessionLocal()
try:
    user = db.query(User).filter_by(telegram_id=107544557).first()
    if user:
        print(f'authorized={user.is_authorized}')
    else:
        print('Пользователь не найден')
finally:
    db.close()
"
```

## Если все еще не работает

1. **Проверьте логи Render**:
   - Dashboard → ваш Web Service → **Logs**
   - Ищите ошибки связанные с БД

2. **Проверьте логи бота**:
   ```bash
   tail -f logs/bot.log
   ```

3. **Убедитесь, что PostgreSQL URL правильный**:
   - Проверьте, что URL скопирован полностью
   - Проверьте, что используется тот же URL в Render и локально

## Готово! 🎉

После настройки PostgreSQL оба сервиса (auth на Render и бот локально) будут использовать одну БД, и авторизация будет работать!

---

**Это единственный способ решить проблему!** SQLite на Render не работает, поэтому PostgreSQL обязателен.
