# 🗄️ Настройка PostgreSQL на Render (ПОШАГОВО)

## Проблема
SQLite на Render не работает → авторизация не сохраняется → бот не видит авторизацию.

## Решение: PostgreSQL на Render

### Шаг 1: Создайте PostgreSQL базу данных

1. Откройте https://dashboard.render.com
2. Нажмите **"New +"** (вверху справа)
3. Выберите **"PostgreSQL"**
4. Заполните форму:
   - **Name**: `telegram-summary-bot-db`
   - **Database**: оставьте по умолчанию
   - **User**: оставьте по умолчанию
   - **Region**: выберите ближайший (например, Frankfurt)
   - **PostgreSQL Version**: оставьте последнюю
   - **Plan**: **Free** (для начала)
5. Нажмите **"Create Database"**

⏱️ Подождите 1-2 минуты, пока база создается.

### Шаг 2: Получите Database URL

1. После создания откройте созданную базу данных
2. В разделе **"Connections"** найдите **"Internal Database URL"**
3. Нажмите на иконку копирования рядом с URL
4. URL будет выглядеть так:
   ```
   postgresql://user:password@host:5432/dbname
   ```
5. **Скопируйте этот URL!** Он понадобится в следующих шагах.

### Шаг 3: Обновите переменные окружения в Render Web Service

1. В Render Dashboard найдите ваш Web Service: `telegram-summary-bot-auth`
2. Откройте его
3. Перейдите в раздел **"Environment"** (в меню слева)
4. Найдите переменную `DATABASE_URL`
5. Нажмите на нее для редактирования
6. Замените значение на PostgreSQL URL из шага 2
7. Нажмите **"Save Changes"**

✅ Render автоматически перезапустит сервис (подождите 2-5 минут).

### Шаг 4: Обновите локальный .env

1. Откройте файл `.env` в проекте `telegram_summary_bot`
2. Найдите строку `DATABASE_URL=`
3. Замените на PostgreSQL URL из шага 2:
   ```bash
   DATABASE_URL=postgresql://user:password@host:5432/dbname
   ```
4. **ВАЖНО**: Используйте тот же URL, что и на Render!

### Шаг 5: Перезапустите бота

```bash
cd telegram_summary_bot
pkill -f "python3 main.py"
python3 main.py
```

### Шаг 6: Проверьте авторизацию

1. Отправьте боту `/start` (если еще не отправляли)
2. Отправьте `/auth`
3. Откройте ссылку: `https://gorbunovdmitry.github.io/my_sum_bot/`
4. Нажмите "Login with Telegram"
5. После успешной авторизации отправьте `/status`
6. Должно показать: **"✅ Авторизован"**

## Проверка работы

### Проверка API на Render:

```bash
# Health check
curl https://telegram-summary-bot-auth.onrender.com/health

# Проверка авторизации (замените USER_ID на ваш Telegram ID)
curl https://telegram-summary-bot-auth.onrender.com/check-auth/107544557
```

Ваш Telegram ID можно узнать у @userinfobot в Telegram.

### Проверка локальной БД:

```bash
cd telegram_summary_bot
python3 -c "
from database import SessionLocal, User
db = SessionLocal()
try:
    user = db.query(User).filter_by(telegram_id=107544557).first()
    if user:
        print(f'authorized={user.is_authorized}, enabled={user.is_enabled}')
    else:
        print('Пользователь не найден')
finally:
    db.close()
"
```

## Если что-то не работает

1. **Проверьте логи Render**:
   - Dashboard → ваш Web Service → **Logs**
   - Ищите ошибки связанные с БД

2. **Проверьте логи бота**:
   ```bash
   tail -f logs/bot.log
   ```

3. **Убедитесь, что PostgreSQL URL правильный**:
   - Проверьте, что URL скопирован полностью
   - Проверьте, что используется тот же URL в Render и локально

4. **Проверьте, что таблицы созданы**:
   - Таблицы должны создаться автоматически при первом запуске
   - Если нет, запустите бота локально - он создаст таблицы

## Готово! 🎉

Теперь оба сервиса (auth на Render и бот локально) используют одну PostgreSQL БД, и авторизация должна работать корректно!
