# 🚀 Быстрая настройка ngrok (5 минут)

## Шаг 1: Установка ngrok

```bash
# Если есть Homebrew:
brew install ngrok/ngrok/ngrok

# Или скачайте с https://ngrok.com/download
```

## Шаг 2: Получите токен

1. Откройте https://dashboard.ngrok.com/get-started/your-authtoken
2. Зарегистрируйтесь (если нужно)
3. Скопируйте токен

## Шаг 3: Настройте токен

```bash
ngrok config add-authtoken ВАШ_ТОКЕН
```

## Шаг 4: Запустите туннель

В **новом терминале** выполните:

```bash
cd "/Users/dmitry/Desktop/Cursor/просто вопросы/telegram_summary_bot"
ngrok http 5000
```

Вы увидите что-то вроде:
```
Forwarding  https://abc123.ngrok-free.app -> http://localhost:5000
```

**Скопируйте URL**: `https://abc123.ngrok-free.app`

## Шаг 5: Зарегистрируйте домен в BotFather

1. Откройте Telegram → @BotFather
2. Отправьте: `/setdomain`
3. Выберите бота: `My_sum_test_bot`
4. Введите домен (без https://): `abc123.ngrok-free.app`

## Шаг 6: Обновите .env

```bash
cd "/Users/dmitry/Desktop/Cursor/просто вопросы/telegram_summary_bot"
echo "AUTH_SERVER_URL=https://abc123.ngrok-free.app/auth" >> .env
```

(Замените `abc123.ngrok-free.app` на ваш ngrok URL)

## Шаг 7: Перезапустите бота

```bash
pkill -f "python3 main.py"
python3 main.py > /tmp/bot_ngrok.log 2>&1 &
```

## Шаг 8: Проверьте

1. Отправьте `/auth` боту
2. Откройте ссылку (теперь это будет ngrok URL)
3. Кнопка "Login with Telegram" должна появиться!

---

## ⚠️ Важно

- **ngrok должен быть запущен** пока работает бот
- При каждом перезапуске ngrok (бесплатный план) URL может измениться
- Для постоянного URL нужен платный план ngrok или свой домен

## 🔄 Если URL изменился

Просто повторите шаги 5-7 с новым URL.
