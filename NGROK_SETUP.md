# 🌐 Настройка ngrok для Telegram Login Widget

## Проблема

Telegram требует, чтобы домен был зарегистрирован в BotFather. Для локальной разработки нужно использовать публичный URL через ngrok.

## Шаг 1: Установка ngrok

### Вариант 1: Homebrew (рекомендуется)
```bash
brew install ngrok/ngrok/ngrok
```

### Вариант 2: Скачать вручную
1. Перейдите на https://ngrok.com/download
2. Скачайте для macOS
3. Распакуйте и переместите в `/usr/local/bin`:
   ```bash
   sudo mv ngrok /usr/local/bin/
   ```

## Шаг 2: Получение токена

1. Зарегистрируйтесь на https://dashboard.ngrok.com
2. Перейдите в раздел "Your Authtoken"
3. Скопируйте токен

## Шаг 3: Настройка ngrok

```bash
ngrok config add-authtoken YOUR_TOKEN
```

## Шаг 4: Запуск туннеля

В отдельном терминале запустите:

```bash
cd telegram_summary_bot
./setup_ngrok.sh
```

Или вручную:
```bash
ngrok http 5000
```

Вы получите публичный URL вида:
```
https://xxxx-xx-xx-xx-xx.ngrok-free.app
```

## Шаг 5: Регистрация домена в BotFather

1. Откройте Telegram и найдите @BotFather
2. Отправьте команду: `/setdomain`
3. Выберите вашего бота: `My_sum_test_bot`
4. Введите домен (без https://):
   ```
   xxxx-xx-xx-xx-xx.ngrok-free.app
   ```

## Шаг 6: Обновление URL в боте

После получения ngrok URL, обновите его в коде или используйте переменную окружения.

## Альтернативное решение (без ngrok)

Если не хотите использовать ngrok, можно:
1. Разместить веб-сервер на VPS с публичным доменом
2. Использовать другой способ авторизации (но Telegram Login Widget требует публичный домен)

## Проверка

После настройки:
1. Откройте в браузере: `https://xxxx-xx-xx-xx-xx.ngrok-free.app/auth`
2. Кнопка "Login with Telegram" должна появиться
3. После авторизации данные отправятся на сервер
