# 🚀 Деплой через GitHub Pages

## Почему не нужен ngrok?

Вы правы! Ngrok нужен только для локальной разработки. Для продакшена можно использовать:
- GitHub Pages (бесплатно)
- Любой другой хостинг с доменом
- VPS с доменом

## ⚠️ Важно: Нужны два компонента

1. **Фронтенд** (HTML страница) - можно на GitHub Pages
2. **Бэкенд** (обработка callback) - нужен сервер с Python

## 📋 План деплоя

### Вариант A: GitHub Pages + отдельный бэкенд

#### Шаг 1: Фронтенд на GitHub Pages

1. Создайте репозиторий на GitHub (например: `telegram-auth-frontend`)
2. Загрузите `auth_page_github.html` как `index.html`
3. Включите GitHub Pages в Settings → Pages
4. Получите URL: `https://ваш-username.github.io/telegram-auth-frontend/`

#### Шаг 2: Настройте бэкенд

Ваш `auth_server.py` должен быть доступен публично:
- **Railway** (railway.app) - бесплатный план
- **Render** (render.com) - бесплатный план  
- **Fly.io** (fly.io) - бесплатный план
- **VPS** с доменом

#### Шаг 3: Обновите HTML

В `auth_page_github.html` измените:
```javascript
const BACKEND_URL = 'https://ваш-бэкенд-домен.com/auth/callback';
```

#### Шаг 4: Зарегистрируйте домен в BotFather

1. @BotFather → `/setdomain`
2. Выберите: `My_sum_test_bot`
3. Введите: `ваш-username.github.io`

---

### Вариант B: Все на одном сервере

Если у вас есть VPS/хостинг с доменом:

1. Загрузите `auth_page.html` на сервер
2. Запустите `auth_server.py` на сервере
3. Зарегистрируйте домен в BotFather

---

## 🎯 Рекомендация

**Для быстрого старта:**
1. Фронтенд → GitHub Pages (бесплатно, 2 минуты)
2. Бэкенд → Railway.app (бесплатно, 5 минут)

**Для продакшена:**
- Используйте один домен для всего
- Разместите на VPS или хостинге

---

## 📝 Инструкция для Railway (бэкенд)

1. Зарегистрируйтесь на railway.app
2. Создайте новый проект
3. Подключите GitHub репозиторий
4. Railway автоматически определит Python
5. Добавьте переменные окружения из `.env`
6. Получите публичный URL

Готов помочь настроить любой вариант!
