# 🌐 Настройка через GitHub Pages

## Архитектура решения

1. **GitHub Pages** - для фронтенда (HTML страница авторизации)
2. **Ваш сервер** - для бэкенда (обработка callback)

## Вариант 1: GitHub Pages + ваш сервер (рекомендуется)

### Шаг 1: Создайте репозиторий на GitHub

1. Создайте новый репозиторий (например: `telegram-auth-page`)
2. Включите GitHub Pages в настройках репозитория

### Шаг 2: Подготовьте HTML файл

Скопируйте `auth_page.html` в репозиторий и обновите URL callback:

```html
<!-- В auth_page.html измените URL на ваш сервер -->
fetch('https://ваш-домен.com/auth/callback', {
```

### Шаг 3: Загрузите на GitHub Pages

```bash
git init
git add auth_page.html
git commit -m "Add auth page"
git branch -M main
git remote add origin https://github.com/ваш-username/telegram-auth-page.git
git push -u origin main
```

### Шаг 4: Настройте GitHub Pages

1. Settings → Pages
2. Source: Deploy from a branch
3. Branch: main
4. Получите URL: `https://ваш-username.github.io/telegram-auth-page/auth_page.html`

### Шаг 5: Зарегистрируйте домен в BotFather

1. Откройте @BotFather
2. `/setdomain` → `My_sum_test_bot`
3. Введите: `ваш-username.github.io`

### Шаг 6: Настройте бэкенд

Ваш Flask сервер (`auth_server.py`) должен быть доступен публично:
- На VPS с доменом
- Через другой туннель
- Или используйте сервис типа Railway, Render, Fly.io

---

## Вариант 2: Полностью на GitHub Pages (только фронтенд)

Если у вас нет отдельного сервера для бэкенда, можно использовать GitHub Pages + GitHub Actions для обработки callback, но это сложнее.

---

## Вариант 3: Использовать существующий хостинг

Если у вас уже есть хостинг с доменом:
1. Загрузите `auth_page.html` на хостинг
2. Загрузите `auth_server.py` на сервер с Python
3. Зарегистрируйте домен в BotFather

---

## Рекомендация

Для простоты используйте:
- **GitHub Pages** для фронтенда (бесплатно)
- **Railway/Render/Fly.io** для бэкенда (бесплатные планы)

Или разместите все на одном VPS с доменом.
